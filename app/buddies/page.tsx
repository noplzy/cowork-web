"use client";

import type { FormEvent } from "react";
import Link from "next/link";
import { useEffect, useMemo, useState } from "react";
import { getClientSessionSnapshot } from "@/lib/clientAuth";
import { Image20SidebarShell } from "@/components/image20/Image20Chrome";
import { IdentityRequirementPanel } from "@/components/auth/IdentityRequirementPanel";

type Tab = "market" | "services" | "slots" | "bookings" | "provider";
type Service = { id: string; title: string; summary: string; price_per_hour_twd: number; buddy_category: string; delivery_mode: string; status?: string; open_slots_count?: number; provider_profile?: { handle?: string | null; display_name?: string | null; is_professional_buddy?: boolean | null } | null };
type Slot = { id: string; service_id: string; starts_at: string; ends_at: string; slot_status: string; note?: string | null };
type Booking = { id: string; service_id: string; slot_id: string | null; buyer_user_id: string; provider_user_id: string; booking_status: string; payment_status: string; scheduled_start_at: string; scheduled_end_at: string; total_amount_twd: number; linked_room_id?: string | null; linked_room_invite_code?: string | null; service?: { title?: string; summary?: string } | null };

const serviceDefaults = { title: "", summary: "", description: "", buddy_category: "focus", interaction_style: "guided", delivery_mode: "remote", visibility: "public", tag_list_input: "", price_per_hour_twd: 300, accepts_new_users: true, accepts_last_minute: false, availability_note: "", status: "draft" };
function formatDateTime(value?: string | null) { if (!value) return "—"; const date = new Date(value); if (Number.isNaN(date.getTime())) return "—"; return new Intl.DateTimeFormat("zh-TW", { month: "2-digit", day: "2-digit", hour: "2-digit", minute: "2-digit" }).format(date); }
function localValue(date: Date) { return new Date(date.getTime() - date.getTimezoneOffset() * 60000).toISOString().slice(0, 16); }
function nextHourValue() { const date = new Date(); date.setMinutes(0, 0, 0); date.setHours(date.getHours() + 1); return localValue(date); }
function addHoursLocal(value: string, hours: number) { const date = new Date(value); date.setHours(date.getHours() + hours); return localValue(date); }

export default function BuddiesPage() {
  const [email, setEmail] = useState("");
  const [token, setToken] = useState("");
  const [tab, setTab] = useState<Tab>("market");
  const [identity, setIdentity] = useState<any>(null);
  const [services, setServices] = useState<Service[]>([]);
  const [myServices, setMyServices] = useState<Service[]>([]);
  const [bookings, setBookings] = useState<Booking[]>([]);
  const [slots, setSlots] = useState<Slot[]>([]);
  const [msg, setMsg] = useState("");
  const [loading, setLoading] = useState(true);
  const [serviceForm, setServiceForm] = useState(serviceDefaults);
  const [slotForm, setSlotForm] = useState({ service_id: "", starts_at: nextHourValue(), ends_at: addHoursLocal(nextHourValue(), 1), note: "" });
  const [providerForm, setProviderForm] = useState({ display_title: "", experience_summary: "", service_boundaries: "" });
  const authHeaders = useMemo(() => token ? { Authorization: `Bearer ${token}` } : {}, [token]);

  useEffect(() => { let cancelled = false; (async () => { const session = await getClientSessionSnapshot().catch(() => null); if (cancelled) return; setEmail(session?.email ?? ""); setToken(session?.accessToken ?? ""); })(); return () => { cancelled = true; }; }, []);
  useEffect(() => { loadAll().catch((error) => setMsg(error.message)); /* eslint-disable-next-line react-hooks/exhaustive-deps */ }, [token]);

  async function readJson(path: string, init: RequestInit = {}) { const headers = new Headers(init.headers); Object.entries(authHeaders).forEach(([key, value]) => headers.set(key, value)); if (init.body && !headers.has("Content-Type")) headers.set("Content-Type", "application/json"); const res = await fetch(path, { ...init, headers, cache: "no-store" }); const body = await res.json().catch(() => ({})); if (!res.ok) throw new Error(body.error || `Request failed: ${res.status}`); return body; }
  async function loadAll() { setLoading(true); try { const marketplace = await readJson("/api/buddies/services"); setServices(marketplace.services || []); if (token) { const identityPayload = await readJson("/api/account/identity/status").catch(() => ({ identity: null })); setIdentity(identityPayload.identity || null); const [mine, bookingPayload, slotPayload, applicationPayload] = await Promise.all([readJson("/api/buddies/services?mine=1").catch(() => ({ services: [] })), readJson("/api/buddies/bookings").catch(() => ({ bookings: [] })), readJson("/api/buddies/slots?mine=1").catch(() => ({ slots: [] })), readJson("/api/buddies/provider/application").catch(() => ({ applications: [] }))]); setMyServices(mine.services || []); setBookings(bookingPayload.bookings || []); setSlots(slotPayload.slots || []); const app = applicationPayload.applications?.[0]; if (app) setProviderForm({ display_title: app.display_title || "", experience_summary: app.experience_summary || "", service_boundaries: app.service_boundaries || "" }); } setMsg(""); } finally { setLoading(false); } }
  async function saveService(event: FormEvent<HTMLFormElement>) { event.preventDefault(); if (!token) return setMsg("請先登入後再管理服務。"); setMsg("正在儲存服務…"); try { await readJson("/api/buddies/services", { method: "POST", body: JSON.stringify(serviceForm) }); setServiceForm(serviceDefaults); await loadAll(); setTab("services"); setMsg("已儲存服務。若要公開出現在市場，服務狀態需為上架中。"); } catch (error: any) { setMsg(error.message || "儲存服務失敗。"); } }
  async function saveSlot(event: FormEvent<HTMLFormElement>) { event.preventDefault(); if (!token) return setMsg("請先登入後再管理時段。"); setMsg("正在建立時段…"); try { await readJson("/api/buddies/slots", { method: "POST", body: JSON.stringify(slotForm) }); setSlotForm({ service_id: slotForm.service_id, starts_at: nextHourValue(), ends_at: addHoursLocal(nextHourValue(), 1), note: "" }); await loadAll(); setMsg("已建立可預約時段。"); } catch (error: any) { setMsg(error.message || "建立時段失敗。"); } }
  async function updateBooking(bookingId: string, action: "accept" | "decline" | "cancel" | "complete") { setMsg("正在更新預約…"); try { await readJson(`/api/buddies/bookings/${bookingId}`, { method: "PATCH", body: JSON.stringify({ action }) }); await loadAll(); setMsg("已更新預約。"); } catch (error: any) { setMsg(error.message || "更新預約失敗。"); } }
  async function submitProviderApplication(event: FormEvent<HTMLFormElement>) { event.preventDefault(); if (!token) return setMsg("請先登入後再申請成為安感夥伴。"); setMsg("正在送出安感夥伴申請…"); try { await readJson("/api/buddies/provider/application", { method: "POST", body: JSON.stringify({ ...providerForm, submit: true }) }); await loadAll(); setMsg("已送出安感夥伴申請。"); } catch (error: any) { setMsg(error.message || "送出申請失敗。"); } }

  return <Image20SidebarShell title="安感夥伴" email={email} lead="Buddies 是有邊界、可預約、可審核的陪伴服務。使用功能前需手機綁定；Buddies 操作需實名審核。"><div className="i20-page" data-image20-dom-page="buddies-v118-service-detail-gated"><section className="i20-panel dark" style={{ backgroundImage: "linear-gradient(90deg,rgba(7,21,29,.95),rgba(7,21,29,.55)),url(/site-assets/image20/hero/brand-hero-evening-shared-presence.png)", backgroundSize: "cover", backgroundPosition: "center", minHeight: 250 }}><span className="i20-kicker">Buddies</span><h2 className="i20-serif" style={{ fontSize: 44 }}>找安感夥伴，或開始提供陪伴服務。</h2><p>Buddies 不是交友牆，而是把陪伴服務整理成可以被理解、預約與審核的入口。</p><div className="i20-actions-row">{(["market", "services", "slots", "bookings", "provider"] as Tab[]).map((item) => <button key={item} className={`i20-btn ${tab === item ? "peach" : "ghost"}`} onClick={() => setTab(item)}>{item === "market" ? "找夥伴" : item === "services" ? "我的服務" : item === "slots" ? "時段" : item === "bookings" ? "預約" : "夥伴申請"}</button>)}</div></section>{msg ? <div className="i20-panel" style={{ marginTop: 18 }}>{msg}</div> : null}{tab !== "market" ? <IdentityRequirementPanel identity={identity} requireRealName /> : null}{tab === "market" ? <section className="i20-grid four" style={{ marginTop: 18 }}>{loading ? <div className="i20-card">讀取中…</div> : services.length ? services.map((service) => <article className="i20-card" key={service.id}><div className="i20-avatar">{service.provider_profile?.display_name?.[0] || "島"}</div><span className="i20-kicker">{service.provider_profile?.is_professional_buddy ? "Verified Buddy" : "Buddy Service"}</span><h3>{service.title}</h3><p>{service.summary}</p><div className="i20-chip-row"><span className="i20-chip">NT${service.price_per_hour_twd}/hr</span><span className="i20-chip">{service.buddy_category}</span><span className="i20-chip">{service.delivery_mode}</span><span className="i20-chip">{service.open_slots_count || 0} 時段</span></div><div className="i20-actions-row"><Link className="i20-btn light" href={`/buddies/services/${service.id}`}>查看服務詳情</Link>{service.provider_profile?.handle ? <Link className="i20-btn ghost" href={`/u/${service.provider_profile.handle}`}>公開檔案</Link> : null}</div></article>) : <div className="i20-card">目前沒有上架服務。</div>}</section> : null}{tab === "services" ? <section className="i20-room-layout" style={{ marginTop: 18 }}><article className="i20-panel"><span className="i20-kicker">Provider Console</span><h3>上架我的陪伴服務</h3><form className="i20-list" onSubmit={saveService}><div className="i20-field"><label>服務標題</label><input className="i20-input" value={serviceForm.title} onChange={(event) => setServiceForm({ ...serviceForm, title: event.target.value })} /></div><div className="i20-field"><label>摘要</label><input className="i20-input" value={serviceForm.summary} onChange={(event) => setServiceForm({ ...serviceForm, summary: event.target.value })} /></div><div className="i20-field"><label>詳細說明</label><textarea className="i20-textarea" value={serviceForm.description} onChange={(event) => setServiceForm({ ...serviceForm, description: event.target.value })} /></div><div className="i20-form-grid"><select className="i20-select" value={serviceForm.buddy_category} onChange={(event) => setServiceForm({ ...serviceForm, buddy_category: event.target.value })}><option value="focus">專注陪伴</option><option value="life">生活陪伴</option><option value="share">主題分享</option><option value="support">溫和支持</option><option value="hobby">興趣同好</option><option value="pro">專業服務</option></select><select className="i20-select" value={serviceForm.status} onChange={(event) => setServiceForm({ ...serviceForm, status: event.target.value })}><option value="draft">草稿</option><option value="active">上架中</option><option value="paused">暫停</option></select><input className="i20-input" type="number" value={serviceForm.price_per_hour_twd} onChange={(event) => setServiceForm({ ...serviceForm, price_per_hour_twd: Number(event.target.value) })} /></div><button className="i20-btn peach" type="submit">儲存服務</button></form></article><article className="i20-panel"><span className="i20-kicker">My Services</span><h3>我的服務</h3><div className="i20-list">{myServices.map((service) => <div className="i20-card" key={service.id}><b>{service.title}</b><p>{service.status}｜NT${service.price_per_hour_twd}/hr</p><Link className="i20-btn light" href={`/buddies/services/${service.id}`}>查看詳情頁</Link></div>)}</div></article></section> : null}{tab === "slots" ? <section className="i20-room-layout" style={{ marginTop: 18 }}><article className="i20-panel"><span className="i20-kicker">Slots</span><h3>建立可預約時段</h3><form className="i20-list" onSubmit={saveSlot}><select className="i20-select" value={slotForm.service_id} onChange={(event) => setSlotForm({ ...slotForm, service_id: event.target.value })}><option value="">選擇服務</option>{myServices.map((service) => <option key={service.id} value={service.id}>{service.title}</option>)}</select><input className="i20-input" type="datetime-local" value={slotForm.starts_at} onChange={(event) => setSlotForm({ ...slotForm, starts_at: event.target.value })} /><input className="i20-input" type="datetime-local" value={slotForm.ends_at} onChange={(event) => setSlotForm({ ...slotForm, ends_at: event.target.value })} /><input className="i20-input" value={slotForm.note} onChange={(event) => setSlotForm({ ...slotForm, note: event.target.value })} placeholder="時段備註" /><button className="i20-btn peach" type="submit">建立時段</button></form></article><article className="i20-panel"><span className="i20-kicker">My Slots</span><h3>我的時段</h3><div className="i20-list">{slots.map((slot) => <div className="i20-card" key={slot.id}><b>{formatDateTime(slot.starts_at)} ～ {formatDateTime(slot.ends_at)}</b><p>{slot.slot_status}｜{slot.note || "—"}</p></div>)}</div></article></section> : null}{tab === "bookings" ? <section className="i20-grid two" style={{ marginTop: 18 }}>{bookings.map((booking) => <article className="i20-card" key={booking.id}><span className="i20-kicker">{booking.booking_status} / {booking.payment_status}</span><h3>{booking.service?.title || booking.service_id}</h3><p>{formatDateTime(booking.scheduled_start_at)} ～ {formatDateTime(booking.scheduled_end_at)}｜NT${booking.total_amount_twd}</p><div className="i20-actions-row"><button className="i20-btn light" onClick={() => updateBooking(booking.id, "accept")}>接受</button><button className="i20-btn ghost" onClick={() => updateBooking(booking.id, "decline")}>婉拒</button><button className="i20-btn ghost" onClick={() => updateBooking(booking.id, "cancel")}>取消</button><button className="i20-btn peach" onClick={() => updateBooking(booking.id, "complete")}>完成</button></div></article>)}</section> : null}{tab === "provider" ? <section className="i20-panel" style={{ marginTop: 18 }}><span className="i20-kicker">Provider Application</span><h3>申請成為安感夥伴</h3><form className="i20-list" onSubmit={submitProviderApplication}><input className="i20-input" value={providerForm.display_title} onChange={(event) => setProviderForm({ ...providerForm, display_title: event.target.value })} placeholder="對外顯示標題" /><textarea className="i20-textarea" value={providerForm.experience_summary} onChange={(event) => setProviderForm({ ...providerForm, experience_summary: event.target.value })} placeholder="經驗摘要" /><textarea className="i20-textarea" value={providerForm.service_boundaries} onChange={(event) => setProviderForm({ ...providerForm, service_boundaries: event.target.value })} placeholder="服務邊界" /><button className="i20-btn peach" type="submit">送出申請</button></form></section> : null}</div></Image20SidebarShell>;
}
