"use client";
import Link from "next/link";import {useEffect,useState} from "react";import {FormalOpsShell,adminOpsNav} from "@/components/formalOps/FormalOpsShell";import {formatDateTime,useAuthedJson} from "@/components/formalOps/useAuthedJson";import styles from "@/components/image20/Image20Auxiliary.module.css";
export default function Page(){const{accessToken,authedFetch}=useAuthedJson("/admin/support");const[tickets,setTickets]=useState<any[]>([]);const[msg,setMsg]=useState("正在讀取客服單…");async function load(){const p=await authedFetch("/api/admin/support/tickets?limit=160");setTickets(p.tickets||[])}useEffect(()=>{if(!accessToken)return;load().then(()=>setMsg("")).catch(e=>setMsg(e.message))},[accessToken]);return <FormalOpsShell activeHref="/admin/support" navItems={adminOpsNav} eyebrow="Support Queue" title="客服單管理" description="客服單不只是訊息，而是使用者信任、退款、房間與安全處理的營運紀錄。" topActions={<button type="button" onClick={()=>load().catch(e=>setMsg(e.message))}>重新整理</button>} dataPage="admin-support-v105">{msg?<div className={styles.accountLoading}>{msg}</div>:null}<section className={styles.accountContentGrid}><article className={styles.accountContentCard}><div className={styles.accountContentHead}><div><span className="i20-kicker">Tickets</span><h3>近期客服單</h3></div></div><div className={styles.accountPreferenceList}>{tickets.map(t=><div key={t.id}><b><Link href={`/admin/support/${t.id}`}>{t.subject}</Link></b><span>{t.category}｜{t.status}｜{t.priority}｜{t.user_id}｜{formatDateTime(t.updated_at)}</span></div>)}</div></article></section></FormalOpsShell>}
