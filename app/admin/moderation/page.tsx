"use client";
import Link from "next/link";import {useEffect,useState} from "react";import {FormalOpsShell,adminOpsNav} from "@/components/formalOps/FormalOpsShell";import {formatDateTime,useAuthedJson} from "@/components/formalOps/useAuthedJson";import styles from "@/components/image20/Image20Auxiliary.module.css";
export default function Page(){const{accessToken,authedFetch}=useAuthedJson("/admin/moderation");const[cases,setCases]=useState<any[]>([]);const[msg,setMsg]=useState("正在讀取治理案件…");async function load(){const p=await authedFetch("/api/admin/moderation/cases?limit=160");setCases(p.cases||[])}useEffect(()=>{if(!accessToken)return;load().then(()=>setMsg("")).catch(e=>setMsg(e.message))},[accessToken]);return <FormalOpsShell activeHref="/admin/moderation" navItems={adminOpsNav} eyebrow="Moderation" title="治理案件" description="檢舉被分流後，應成為可追蹤的治理案件，包含處置、申訴、恢復與 audit log。" dataPage="admin-moderation-v105">{msg?<div className={styles.accountLoading}>{msg}</div>:null}<section className={styles.accountContentGrid}><article className={styles.accountContentCard}><div className={styles.accountContentHead}><div><span className="i20-kicker">Cases</span><h3>治理案件</h3></div></div><div className={styles.accountPreferenceList}>{cases.map(c=><div key={c.id}><b><Link href={`/admin/moderation/${c.id}`}>{c.target_type}｜{c.severity}</Link></b><span>{c.status}｜{c.target_user_id||"no-user"}｜{formatDateTime(c.updated_at)}</span></div>)}</div></article></section></FormalOpsShell>}
