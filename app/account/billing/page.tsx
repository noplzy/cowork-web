"use client";
import Link from "next/link";import {useEffect,useState} from "react";import {FormalOpsShell,accountOpsNav} from "@/components/formalOps/FormalOpsShell";import {formatDateTime,formatTwd,useAuthedJson} from "@/components/formalOps/useAuthedJson";import styles from "@/components/image20/Image20Auxiliary.module.css";
export default function Page(){const{accessToken,authedFetch}=useAuthedJson("/account/billing");const[p,setP]=useState<any>(null);const[msg,setMsg]=useState("正在讀取帳務資料…");useEffect(()=>{if(!accessToken)return;authedFetch("/api/account/billing").then(d=>{setP(d);setMsg("")}).catch(e=>setMsg(e.message))},[accessToken]);const orders=p?.payment_orders||[],ledger=p?.billing_ledger||[],refunds=p?.refund_requests||[],invoices=p?.invoice_events||[],ent=p?.entitlement;return <FormalOpsShell activeHref="/account/billing" navItems={accountOpsNav} eyebrow="Billing Center" title="帳務紀錄" description="付款、權益、退款與發票事件會集中到這裡。" quoteTitle={ent?.plan||"free"} quoteBody={`VIP 到期：${formatDateTime(ent?.vip_until)}`} topActions={<><Link href="/pricing">查看方案</Link><Link href="/account/refunds">申請退款</Link></>} dataPage="account-billing-v105">{msg?<div className={styles.accountLoading}>{msg}</div>:null}<section className={styles.accountMetricGrid}><article className={styles.accountMetricCard}><span className="i20-kicker">Orders</span><h3>付款訂單</h3><b>{orders.length}</b><p>付款成立後會同步權益與帳務事件。</p></article><article className={styles.accountMetricCard}><span className="i20-kicker">Ledger</span><h3>帳務事件</h3><b>{ledger.length}</b><p>付款、權益、退款與人工調整會留下紀錄。</p></article><article className={styles.accountMetricCard}><span className="i20-kicker">Refunds</span><h3>退款申請</h3><b>{refunds.length}</b><p>退款需要客服與帳務審核。</p></article></section><section className={styles.accountContentGrid}><article className={styles.accountContentCard}><div className={styles.accountContentHead}><div><span className="i20-kicker">Payment Orders</span><h3>付款紀錄</h3></div></div><div className={styles.accountPreferenceList}>{orders.map((o:any)=><div key={o.id||o.merchant_trade_no}><b>{o.item_name||o.plan_code||"付款訂單"}｜{formatTwd(o.amount)}</b><span>{o.status}｜{o.merchant_trade_no}｜{formatDateTime(o.created_at)}</span></div>)}{orders.length===0?<div><b>目前沒有付款紀錄。</b><span>完成付款後會出現在這裡。</span></div>:null}</div></article><article className={styles.accountContentCard}><div className={styles.accountContentHead}><div><span className="i20-kicker">Ledger / Invoice</span><h3>帳務與發票事件</h3></div></div><div className={styles.accountPreferenceList}>{ledger.slice(0,8).map((i:any)=><div key={i.id}><b>{i.ledger_type}｜{formatTwd(i.amount_twd)}</b><span>{i.direction}｜{i.description||"—"}｜{formatDateTime(i.occurred_at)}</span></div>)}{invoices.slice(0,4).map((i:any)=><div key={i.id}><b>發票事件｜{i.event_type}</b><span>{i.invoice_number||"尚未開立"}｜{formatDateTime(i.created_at)}</span></div>)}</div></article></section></FormalOpsShell>}
